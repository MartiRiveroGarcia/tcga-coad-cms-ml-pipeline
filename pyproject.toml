[project]
name = "tcga-coad-cms-ml-pipeline"
version = "0.1.0"
description = "Reproducible ML pipeline for CMS classification of TCGA-COAD colorectal cancer"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]
    